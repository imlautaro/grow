import { defineConfig } from 'windicss/helpers'

export default defineConfig({
	theme: {
		container: {
			center: true,
			padding: '1rem',
		},
		fontFamily: {
			sans: ['"Readex Pro"', 'sans-serif'],
		},
	},
})
